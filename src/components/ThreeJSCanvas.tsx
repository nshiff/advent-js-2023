export function ThreeJSCanvas(){

    // a React component designed to manage global state for the ThreeJS 3-D rendering library

    return (
        <div>
        <h3>ThreeJS Canvas</h3>

        </div>
    )
}